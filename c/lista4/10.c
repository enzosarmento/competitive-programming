#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int main() {

  srand(time(NULL));
  int soma, d1, d2;
  printf("%d\n", (rand() % 6) + 1);

  do {

  } while(soma != 7 || soma != 11);

  return 0;
}